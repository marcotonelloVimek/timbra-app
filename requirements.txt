streamlit
pandas
altair
pytz
streamlit-autorefresh
reportlab

# Necessaria per il database esterno Turso (TURSO_DATABASE_URL / TURSO_AUTH_TOKEN),
# cosi' i dati non si perdono ad ogni riavvio su Streamlit Community Cloud. Si usa
# "libsql" (pacchetto ufficiale tursodatabase/libsql-python): su Streamlit Community
# Cloud (Linux, Python standard) si installa da un pacchetto già pronto, senza le
# complicazioni viste in locale su Windows con una versione di Python nuovissima.
libsql
